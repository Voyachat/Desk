/** Test-only Cloud plus Local Capability conformance bundle marker. */

/**
 * The deterministic conformance composition is declared by `cordis.patch.yml`.
 * @returns Nothing.
 */
export function apply(): void {}
