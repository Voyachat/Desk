/** Fixed identities written by the Codex driver. */

/** Session runtime id served by this package. */
export const CODEX_RUNTIME = 'codex'

/** Assistant provenance provider used for Codex app-server output. */
export const CODEX_PROVIDER = 'codex-agent'

/** Default process-tree termination grace. */
export const DEFAULT_DISPOSE_GRACE_MS = 3_000
