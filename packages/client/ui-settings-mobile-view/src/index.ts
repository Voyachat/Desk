/** Host loader entry for the remote-view settings implementation exported from `./client`. */

/** Host plugin body — the mobile-view Host package owns runtime behavior. */
export function apply(): void {}
