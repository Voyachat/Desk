# Add the self-referential Cordis tools without changing the base ACP tool
# presentation mode.
- id: base
  name: '@voyaseek-ai/cordis-plugin-include'
  config:
    path: ./cordis.yml
    patches:
      - insert:
          - id: cordis-host-runner
            name: '@voyaseek-ai/dsh-cordis-host-runner'
          - id: tool-cordis
            name: '@voyaseek-ai/dsh-tool-cordis'
