# AI Staff Desktop Runtime / AI Staff 桌面运行时

This private deploy manifest owns the production dependency closure embedded in the desktop application. `scripts/stage-runtime.mjs` uses the repository's pnpm workspace to deploy production packages, materializes every link, copies the built CLI to `runtime/apps/cli`, removes development artifacts and `node-pty` prebuilds for platforms other than macOS x86_64, and verifies the web assets, AI Staff Client plugin, language-policy runtime chunk, worker entry points, `node-pty` license and target prebuild before atomically replacing `runtime/`.

此私有部署清单负责桌面应用内嵌的生产依赖闭包。`scripts/stage-runtime.mjs` 使用仓库的 pnpm 工作区部署生产包、实体化所有链接、将已构建的 CLI 复制到 `runtime/apps/cli`、删除开发产物和非 macOS x86_64 平台的 `node-pty` prebuild，并在原子替换 `runtime/` 前验证 Web 资源、AI Staff Client 插件、language-policy 运行时 chunk、worker 入口、`node-pty` 许可证及目标 prebuild。

The staged directory is copied by Electron Forge as `Contents/Resources/runtime`, outside `app.asar`. Verification rejects a runtime above 470 MiB or 27,000 regular files and reports both measurements as JSON. These budgets cover the complete staged directory; a dependency increase must remain within them or update the constants with a measured packaged-runtime justification. The directory is generated output and must be rebuilt after any DSH runtime package changes. Run staging serially: pnpm's legacy deploy can replace workspace links, so restore the repository's pnpm install before invoking Electron Forge or other workspace commands.

Electron Forge 将暂存目录复制为 `app.asar` 外的 `Contents/Resources/runtime`。验证会拒绝超过 470 MiB 或 27,000 个普通文件的运行时，并以 JSON 报告两项测量结果。这些预算覆盖完整暂存目录；依赖增长必须保持在预算内，或依据实测的打包运行时结果调整常量。该目录属于生成产物，任何 DSH 运行时包变更后都必须重新构建。暂存操作必须串行执行：pnpm 的 legacy deploy 可能替换工作区链接，因此在调用 Electron Forge 或其他工作区命令前应恢复仓库的 pnpm install。

[`startup-policy.json`](./startup-policy.json) is the authoritative coarse desktop startup classification and owns the required-byte budget. The local HTML, CSS, JavaScript, and built Desktop `.js`/`.cjs` files are required before interaction. The complete managed runtime is deferred until the startup shell is shown; the policy does not divide Host plugins into finer phases. Every `node-pty` prebuild except `darwin-x64` is excluded. `node scripts/verify-startup-policy.mjs --required-only` checks emitted startup code during ordinary Desktop compilation; the command without the flag additionally checks the generated runtime before packaging. Both forms emit one JSON summary.

[`startup-policy.json`](./startup-policy.json) 是桌面冷启动粗粒度分类和 required 字节预算的唯一权威文件。本地 HTML、CSS、JavaScript 和构建后的 Desktop `.js`/`.cjs` 文件必须在可交互前就绪。完整 managed runtime 在启动壳显示后才启动；此策略不再细分 Host 插件阶段。除 `darwin-x64` 外的所有 `node-pty` prebuild 均被排除。普通 Desktop 编译使用 `node scripts/verify-startup-policy.mjs --required-only` 检查生成后的启动代码；打包前使用不带参数的命令继续检查生成后的运行时。两种形式都会输出一条 JSON 摘要。
